# 安全与隐私政策

## 1 支持范围

当前维护版本为 `0.5.x`，安全修复会优先进入最新补丁版本

## 2 私密报告

请使用仓库的 **Security → Report a vulnerability** 提交私密报告，不要在公开 Issue、讨论区或拉取请求中粘贴原始遥测、路径、提示词、代码、凭据或个人信息

报告可以包含受影响版本、复现步骤、预期结果和已经完成的脱敏验证，维护者会先确认是否收到，再评估影响与修复范围

## 3 数据边界

遥测默认关闭，需要用户明确启用，历史审计也只在用户运行命令后开始，并要求把派生结果写到公开仓库外

历史分析期间，原始对话只在内存中参与确定性分类，落盘数据只包含 HMAC 化名、固定分类标签、token 数、工具类别和聚合结果，不保存提示词、回复、代码、日志、文件名、路径、URL、邮箱、账号或秘密

逐轮遥测只保存固定结构的本地记录，公开快照会删除项目和机器伪标识并抑制小样本比率

配对评测只保存任务、项目、基线和运行记录的 HMAC 化名，以及路线读回、数字评分和受控验收字段，评测包不复制原始任务、模型输出、代码、补丁、日志或路径

任务段状态只保存合同和交接内容的 SHA-256 摘要、HMAC 化名、固定检查点、路线及验收计数；交接正文仅在用户明确指定 `--output` 时写入仓库外私有文件，不进入聚合报告或切换评测记录

切换损失评测只保存转换路线、恢复分钟、纠正次数、重复动作、上下文缺失计数、固定评分和受控验收字段，不复制私有交接正文

工具不会主动上传数据，仓库也不提供后台服务、网络收集端点或自动公开功能，用户需要自行保护本地 Codex Home，因为本机随机盐和原始记录保存在该目录内

## 4 主要威胁

- 同一台机器上能够读取用户文件的其他进程，也可能读取本地遥测
- 极少见任务组合即使没有名称，也可能被旁观者通过背景知识推断
- 手工复制原始记录到公开渠道会绕过快照的去标识与小组抑制
- 宿主提供的 token 或工具调用计数可能缺失，也可能与其他宿主口径不同
- 活动 session 会在只读扫描期间继续追加，来源变化不等于分析器改写历史；归档来源应保持稳定
- 对话中的自由文本只在进程内存中短暂存在，同机高权限进程仍可能观察进程内存
- 配对评测依赖操作者提供正确的隔离工作区和对应 Codex session，脚本只能验证读回路线和结构化结果
- 私有交接文件包含经过结构化整理的任务状态，仍可能具有项目敏感性；用户需要限制文件权限并在交接完成后按照自身保留政策处理
- 路线读回只能证明目标任务公开的模型和 effort 与交接一致，不能证明模型完整继承了前一任务的内部推理状态

## 5 用户控制

用户可以随时运行 `disable` 停止新记录，使用环境变量 `MODEL_EFFORT_ROUTER_TELEMETRY=off` 紧急停止，或通过带固定确认短语的 `purge` 永久删除本地数据
