# 安全与隐私政策

## 1 支持范围

当前维护版本为 `0.2.x`。安全修复会优先进入最新补丁版本。

## 2 私密报告

请使用仓库的 **Security → Report a vulnerability** 提交私密报告。不要在公开 Issue、讨论区或拉取请求中粘贴原始遥测、路径、提示词、代码、凭据或个人信息。

报告可以包含受影响版本、复现步骤、预期结果和已经完成的脱敏验证。维护者会先确认是否收到，再评估影响与修复范围。

## 3 数据边界

遥测默认关闭，需要用户明确启用。运行时只保存固定结构的本地记录，公开快照会删除项目和机器伪标识并抑制小样本比率。

工具不会主动上传数据。仓库也不提供后台服务、网络收集端点或自动公开功能。用户需要自行保护本地 Codex Home，因为本机随机盐和原始记录保存在该目录内。

## 4 主要威胁

- 同一台机器上能够读取用户文件的其他进程，也可能读取本地遥测。
- 极少见任务组合即使没有名称，也可能被旁观者通过背景知识推断。
- 手工复制原始记录到公开渠道会绕过快照的去标识与小组抑制。
- 宿主提供的 token 或工具调用计数可能缺失，也可能与其他宿主口径不同。

## 5 用户控制

用户可以随时运行 `disable` 停止新记录，使用环境变量 `MODEL_EFFORT_ROUTER_TELEMETRY=off` 紧急停止，或通过带固定确认短语的 `purge` 永久删除本地数据。
