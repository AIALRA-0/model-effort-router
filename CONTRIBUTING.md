# 贡献指南

## 1 贡献范围

欢迎改进路由规则、隐私边界、结构化字段、测试、兼容性和文档。当前阶段不接受未经真实数据支持的模型胜率、固定成本倍率或“最佳模型”宣传。

## 2 本地验证

提交前依次完成以下步骤。

第一步，验证包结构和隐私约束。

```powershell
# 检查仓库结构、隐私禁区和文档入口。
python scripts/validate_package.py
```

第二步，运行测试。

```powershell
# 运行全部单元测试和命令行端到端测试。
python -m unittest discover -s tests -v
```

第三步，检查 Python 源文件。

```powershell
# 验证所有脚本和测试能够编译。
python -m compileall -q scripts tests
```

## 3 隐私要求

测试夹具必须使用合成数据。提交内容不得包含真实提示词、回复、代码、文件路径、远程地址、用户名、电子邮件、凭据、原始遥测或可反推出项目身份的组合字段。

新增字段前需要同时更新 Schema、遥测政策、测试和去标识快照逻辑。自由文本字段默认不接受。

## 4 变更说明

拉取请求应说明要解决的问题、数据边界变化、验证证据和回滚方式。涉及默认采集状态、删除操作、网络传输或公开快照的变更，需要单独标记并接受安全审查。
